import { ConditionalNumberPipe } from './conditional-number.pipe';

describe('ConditionalNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionalNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
